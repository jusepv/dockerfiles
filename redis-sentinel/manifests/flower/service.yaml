apiVersion: v1
kind: Service
metadata:
  name: oasis-celery-flower
  namespace: haiqv
spec:
  selector:
    app: oasis-celery-flower
  ports:
    - protocol: TCP
      port: 5555
      targetPort: 5555
  type: ClusterIP
