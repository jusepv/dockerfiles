apiVersion: v1
kind: Service
metadata:
  name: oasis-rag-service
  namespace: haiqv
spec:
  type: NodePort
  selector:
    app: oasis-rag-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 32080
